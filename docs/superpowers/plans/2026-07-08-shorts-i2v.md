# 쇼츠 씬 배경 I2V 클라우드 통합 구현 플랜 (Phase 3)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 씬 QA 통과 배경 이미지를 fal.ai I2V(기본 LTX-2 fast)로 모션 클립화해 Remotion 배경으로 재생한다(씬별 KenBurns 폴백, 완전 fail-open).

**Architecture:** 새 모듈 `src/orchestrator/shortsSceneClips.ts`의 `i2vSceneClips()`가 씬 QA 직후 전 씬 병렬로 fal queue API 호출 → `dir/clips/clip_NN.mp4` 저장 → `renderShortsVideoRemotion`이 `opts.clips`를 public/ 스테이징해 `videoSrc`로 전달 → `AutoShorts` Scene 에서 `videoSrc` 있으면 `<Loop>+<OffthreadVideo>`, 없으면 기존 `KenBurnsImage`. ffmpeg 폴백 무변경.

**Tech Stack:** TypeScript(Node 20+ 내장 fetch)·Remotion(기존). 새 npm 의존성 없음.

## Global Constraints

- 게이트: `FAL_KEY` 없거나 `SHORTS_I2V=off` 면 **완전 no-op** — 기존 파이프라인과 동일 경로(clips 전부 null).
- env 기본값: `SHORTS_I2V=fal`, `SHORTS_I2V_MODEL=fal-ai/ltx-2/image-to-video/fast`.
- fal 요청 body(스펙 §4.2 그대로): `{ prompt, image_url: data URI(base64 png), duration: 6, resolution: '1080p', fps: 25, generate_audio: false }`.
- fal queue REST: `POST https://queue.fal.run/{model}` (헤더 `Authorization: Key ${falKey}`) → `{ status_url, response_url }` → status 2초 폴링 `COMPLETED` → response_url GET → `{ video: { url } }` 다운로드.
- 씬당 타임아웃 캡 **120초**(폴링 포함), **재시도 없음**(과금 캡: 편당 최대 8클립). 씬별 실패는 그 씬만 null(스틸 폴백), 전체 try/catch fail-open, `signal` 취소 존중.
- Remotion 루프: 클립 6초 × 30fps 컴포지션 = `CLIP_FRAMES = 180` 프레임 루프. `OffthreadVideo`는 `muted` + `objectFit: 'cover'`.
- ffmpeg 폴백(`shortsRender.ts`)·씬 QA·SRT·TTS 무변경.
- 빌드/테스트: `npx tsc --noEmit && npx tsc -p remotion/tsconfig.json`(둘 다 exit 0), `npx vitest run <경로>`.
- 커밋 직전 `git status --short`로 내 파일만 스테이징 확인 — 병렬 세션의 data/ 파일 add 금지(`git add -A`/`git add .` 금지). 현재 브랜치는 **main**(사용자 지시) — main 에 직접 커밋한다.

---

### Task 1: CONFIG 확장 + shortsSceneClips 모듈 + 순수 헬퍼 테스트

**Files:**
- Modify: `src/config.ts` (인터페이스 ~112행, 객체 ~228행)
- Create: `src/orchestrator/shortsSceneClips.ts`
- Create: `src/orchestrator/shortsSceneClips.test.ts`

**Interfaces:**
- Produces:
  - `CONFIG.falKey: string`, `CONFIG.shortsI2v: 'fal' | 'off'`, `CONFIG.shortsI2vModel: string`
  - `interface SceneClipsResult { clips: Array<string | null>; issues: string[] }`
  - `buildMotionPrompt(scenePrompt: string): string`
  - `extractVideoUrl(json: unknown): string | null`
  - `i2vSceneClips(opts: { dir: string; images: Array<string | null>; scenePrompts: string[]; signal?: AbortSignal }): Promise<SceneClipsResult>`

- [ ] **Step 1: Write the failing tests**

`src/orchestrator/shortsSceneClips.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildMotionPrompt, extractVideoUrl } from './shortsSceneClips';

describe('buildMotionPrompt — 원본 + 모션 접미(순수)', () => {
  it('원본 포함 + 시네마틱 모션·무텍스트 문구', () => {
    const p = buildMotionPrompt('a plant by the window');
    expect(p).toContain('a plant by the window');
    expect(p).toContain('cinematic motion');
    expect(p).toContain('no text');
  });
});
describe('extractVideoUrl — 정상/결측/이형 방어(순수)', () => {
  it('video.url 이 http 문자열일 때만 통과, 그 외 null', () => {
    expect(extractVideoUrl({ video: { url: 'https://x/y.mp4' } })).toBe('https://x/y.mp4');
    expect(extractVideoUrl({ video: {} })).toBeNull();
    expect(extractVideoUrl({})).toBeNull();
    expect(extractVideoUrl(null)).toBeNull();
    expect(extractVideoUrl({ video: { url: 42 } })).toBeNull();
    expect(extractVideoUrl({ video: { url: 'ftp://x' } })).toBeNull();
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/orchestrator/shortsSceneClips.test.ts`
Expected: FAIL — "Cannot find module './shortsSceneClips'".

- [ ] **Step 3: Extend CONFIG**

`src/config.ts` 인터페이스 — 기존(112행 부근):
```ts
  /** 쇼츠 렌더 엔진 — 'remotion'(모션그래픽, 기본) | 'ffmpeg'(슬라이드쇼 폴백/강제). */
  readonly shortsRenderer: 'remotion' | 'ffmpeg';
```
을 다음으로 교체:
```ts
  /** 쇼츠 렌더 엔진 — 'remotion'(모션그래픽, 기본) | 'ffmpeg'(슬라이드쇼 폴백/강제). */
  readonly shortsRenderer: 'remotion' | 'ffmpeg';
  /** fal.ai API 키 — 쇼츠 씬 배경 I2V(Phase 3). 비면 I2V 완전 비활성(스틸 폴백). */
  readonly falKey: string;
  /** 쇼츠 I2V 스위치 — 'fal'(기본, 키 있을 때만 동작) | 'off'. */
  readonly shortsI2v: 'fal' | 'off';
  /** fal I2V 모델 ID — env 한 줄로 Wan 등 교체. */
  readonly shortsI2vModel: string;
```
객체 — 기존(228행 부근):
```ts
  shortsRenderer: env('SHORTS_RENDERER', 'remotion') === 'ffmpeg' ? 'ffmpeg' : 'remotion',
```
을 다음으로 교체:
```ts
  shortsRenderer: env('SHORTS_RENDERER', 'remotion') === 'ffmpeg' ? 'ffmpeg' : 'remotion',
  falKey: env('FAL_KEY', ''),
  shortsI2v: env('SHORTS_I2V', 'fal') === 'off' ? 'off' : 'fal',
  shortsI2vModel: env('SHORTS_I2V_MODEL', 'fal-ai/ltx-2/image-to-video/fast'),
```

- [ ] **Step 4: Write the module**

`src/orchestrator/shortsSceneClips.ts`:
```ts
/**
 * 쇼츠 씬 배경 I2V — 씬 QA 통과 이미지를 fal.ai I2V 모델(기본 LTX-2 fast)로 모션 클립(mp4)화.
 * 씬별 병렬, 실패 씬은 null(KenBurns 스틸 폴백), 전량 try/catch fail-open — 클립이 없어도
 * 쇼츠는 항상 완성. FAL_KEY 없거나 SHORTS_I2V=off 면 no-op. 씬당 1회·재시도 없음(과금 캡).
 */
import fs from 'node:fs';
import path from 'node:path';
import { CONFIG } from '../config';

export interface SceneClipsResult { clips: Array<string | null>; issues: string[] }

/** 모션 프롬프트 — 씬 프롬프트 + 고정 모션·무텍스트 접미(순수 문자열). */
export function buildMotionPrompt(scenePrompt: string): string {
  return `${scenePrompt} subtle cinematic motion, slow camera drift, natural ambient movement, no text, no captions.`;
}

/** fal 응답에서 video.url 안전 추출(순수) — http(s) 문자열이 아니면 null. */
export function extractVideoUrl(json: unknown): string | null {
  const v = (json as { video?: { url?: unknown } } | null)?.video?.url;
  return typeof v === 'string' && v.startsWith('http') ? v : null;
}

const gate = (): boolean => CONFIG.shortsI2v === 'fal' && !!CONFIG.falKey;

/** fal queue REST 1회 실행 — 제출→폴링(2초)→결과 JSON. 타임아웃·취소·비정상 응답은 throw. */
async function falQueueRun(model: string, body: Record<string, unknown>, signal?: AbortSignal, timeoutMs = 120_000): Promise<unknown> {
  const deadline = Date.now() + timeoutMs;
  const headers = { Authorization: `Key ${CONFIG.falKey}`, 'Content-Type': 'application/json' };
  const sub = await fetch(`https://queue.fal.run/${model}`, { method: 'POST', headers, body: JSON.stringify(body), signal });
  if (!sub.ok) throw new Error(`fal 제출 실패 HTTP ${sub.status}`);
  const q = await sub.json() as { status_url?: string; response_url?: string };
  if (!q.status_url || !q.response_url) throw new Error('fal 큐 응답 이형');
  for (;;) {
    if (signal?.aborted) throw new Error('취소됨');
    if (Date.now() > deadline) throw new Error('fal 타임아웃(120s)');
    const st = await fetch(q.status_url, { headers, signal });
    const sj = await st.json() as { status?: string };
    if (sj.status === 'COMPLETED') break;
    if (sj.status === 'FAILED' || sj.status === 'ERROR') throw new Error(`fal 실패: ${sj.status}`);
    await new Promise((r) => setTimeout(r, 2000));
  }
  const res = await fetch(q.response_url, { headers, signal });
  if (!res.ok) throw new Error(`fal 결과 조회 실패 HTTP ${res.status}`);
  return res.json();
}

export async function i2vSceneClips(opts: {
  dir: string; images: Array<string | null>; scenePrompts: string[]; signal?: AbortSignal;
}): Promise<SceneClipsResult> {
  const out: SceneClipsResult = { clips: opts.images.map(() => null), issues: [] };
  try {
    if (!gate()) return out;
    const clipsDir = path.join(opts.dir, 'clips');
    fs.mkdirSync(clipsDir, { recursive: true });
    await Promise.all(opts.images.map(async (img, i) => {
      if (!img || !fs.existsSync(img)) return;
      try {
        const b64 = fs.readFileSync(img).toString('base64');
        const json = await falQueueRun(CONFIG.shortsI2vModel, {
          prompt: buildMotionPrompt(opts.scenePrompts[i] ?? ''),
          image_url: `data:image/png;base64,${b64}`,
          duration: 6, resolution: '1080p', fps: 25, generate_audio: false,
        }, opts.signal);
        const url = extractVideoUrl(json);
        if (!url) throw new Error('video.url 없음');
        const dl = await fetch(url, { signal: opts.signal });
        if (!dl.ok) throw new Error(`클립 다운로드 실패 HTTP ${dl.status}`);
        const fp = path.join(clipsDir, `clip_${String(i + 1).padStart(2, '0')}.mp4`);
        fs.writeFileSync(fp, Buffer.from(await dl.arrayBuffer()));
        out.clips[i] = fp;
      } catch (e) {
        out.issues.push(`씬${i + 1} I2V 실패(스틸 폴백): ${e instanceof Error ? e.message.slice(0, 60) : e}`);
      }
    }));
  } catch { /* fail-open — 전 씬 스틸 */ }
  return out;
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run src/orchestrator/shortsSceneClips.test.ts`
Expected: PASS (2 describe).

- [ ] **Step 6: Typecheck + 회귀**

Run: `npx tsc --noEmit && npx tsc -p remotion/tsconfig.json; echo "tsc: $?"` → `tsc: 0`.
Run: `npx vitest run src/orchestrator src/tools` → PASS.

- [ ] **Step 7: Commit**

```bash
git status --short   # 아래 3개 파일만 확인
git add src/config.ts src/orchestrator/shortsSceneClips.ts src/orchestrator/shortsSceneClips.test.ts
git commit -m "feat(shorts): fal.ai 씬 배경 I2V 모듈(shortsSceneClips) + CONFIG 게이트 + 순수 헬퍼 테스트"
```

---

### Task 2: runShortsJob 배선

**Files:**
- Modify: `src/orchestrator/shorts.ts` (씬 QA 블록 직후 + 렌더 호출)

**Interfaces:**
- Consumes: `i2vSceneClips`(Task 1).
- Produces: `runShortsJob` 스코프의 `cv: SceneClipsResult` 변수 — Task 3 이 렌더 호출에 `cv.clips` 를 전달한다. **이 태스크에서는 렌더러에 clips 를 넘기지 않는다**(렌더러 opts 에 아직 그 필드가 없어 tsc 가 깨짐 — 전달은 Task 3 의 Step 3).

- [ ] **Step 1: Add import**

`src/orchestrator/shorts.ts` — 기존 `import { qaSceneImages } from './shortsSceneQa';` 바로 아래에 추가:
```ts
import { i2vSceneClips } from './shortsSceneClips';
```

- [ ] **Step 2: Insert I2V call**

기존(씬 QA 블록 끝):
```ts
    if (qa.regenerated) say(`씬 QA — ${qa.regenerated}장 재생성 (${qa.issues.slice(0, 3).join(' · ')})`);
    else if (qa.issues.length) say(`씬 QA — 이슈 ${qa.issues.length}건, 재생성 없음 (${qa.issues.slice(0, 3).join(' · ')})`);
    checkAbort();
```
을 다음으로 교체:
```ts
    if (qa.regenerated) say(`씬 QA — ${qa.regenerated}장 재생성 (${qa.issues.slice(0, 3).join(' · ')})`);
    else if (qa.issues.length) say(`씬 QA — 이슈 ${qa.issues.length}건, 재생성 없음 (${qa.issues.slice(0, 3).join(' · ')})`);
    checkAbort();

    // 3-c) 씬 배경 I2V — fal 클립화(키 없으면 no-op, 실패 씬은 스틸 폴백, fail-open).
    const cv = await i2vSceneClips({
      dir, images, scenePrompts: bgDraft.imageSlots.map((s) => s.prompt), signal: opts.signal,
    });
    const clipCount = cv.clips.filter(Boolean).length;
    if (clipCount) say(`씬 I2V — ${clipCount}/${plan.scenes.length}장 클립화${cv.issues.length ? ` (${cv.issues[0]})` : ''}`);
    else if (cv.issues.length) say(`씬 I2V — 클립 0장 (${cv.issues.slice(0, 2).join(' · ')})`);
    checkAbort();
```

- [ ] **Step 3: Verify types + tests**

Run: `npx tsc --noEmit && npx tsc -p remotion/tsconfig.json; echo "tsc: $?"` → `tsc: 0`.
Run: `npx vitest run src/orchestrator src/tools` → PASS. (cv 는 아직 렌더러로 전달되지 않음 — Task 3 에서 배선. `cv` 미사용 경고가 없도록 say 에서 사용 중.)

- [ ] **Step 4: Commit**

```bash
git status --short   # src/orchestrator/shorts.ts 만
git add src/orchestrator/shorts.ts
git commit -m "feat(shorts): runShortsJob 씬 배경 I2V 클립화 단계 삽입(씬 QA 직후)"
```

---

### Task 3: Remotion 배선(clips→videoSrc→OffthreadVideo) + 최종 검증

**Files:**
- Modify: `src/tools/shortsRenderRemotion.ts` (시그니처 + propScenes)
- Modify: `remotion/AutoShorts.tsx`
- Modify: `src/orchestrator/shorts.ts` (렌더 호출에 clips 전달 — 1줄)

**Interfaces:**
- Consumes: Task 1 `SceneClipsResult.clips`, Task 2 의 `cv` 변수(shorts.ts 스코프에 존재).

- [ ] **Step 1: Extend renderShortsVideoRemotion**

`src/tools/shortsRenderRemotion.ts` — 기존 시그니처:
```ts
export async function renderShortsVideoRemotion(
  dir: string, scenes: ShortsScene[], images: Array<string | null>,
  opts: { voice?: string; instructions?: string; signal?: AbortSignal } = {},
): Promise<ShortsRenderResult> {
```
을 다음으로 교체:
```ts
export async function renderShortsVideoRemotion(
  dir: string, scenes: ShortsScene[], images: Array<string | null>,
  opts: { voice?: string; instructions?: string; signal?: AbortSignal; clips?: Array<string | null> } = {},
): Promise<ShortsRenderResult> {
```
propScenes — 기존:
```ts
    if (p.audioPath) { const dst = `narr_${nn}.mp3`; fs.copyFileSync(p.audioPath, path.join(publicDir, dst)); audioSrc = dst; }
    const sc = scenes[p.index];
    return {
      imageSrc, audioSrc, screenText: p.screenText, durationInFrames: p.durationInFrames,
```
을 다음으로 교체(클립 스테이징 — 실패·부재 씬은 videoSrc null):
```ts
    if (p.audioPath) { const dst = `narr_${nn}.mp3`; fs.copyFileSync(p.audioPath, path.join(publicDir, dst)); audioSrc = dst; }
    let videoSrc: string | null = null;
    const clip = opts.clips?.[p.index];
    if (clip && fs.existsSync(clip)) { const dst = `clip_${nn}.mp4`; fs.copyFileSync(clip, path.join(publicDir, dst)); videoSrc = dst; }
    const sc = scenes[p.index];
    return {
      imageSrc, audioSrc, videoSrc, screenText: p.screenText, durationInFrames: p.durationInFrames,
```

- [ ] **Step 2: Extend AutoShorts**

`remotion/AutoShorts.tsx` — (a) import 교체:
```ts
import { AbsoluteFill, Series, Audio, staticFile } from 'remotion';
```
→
```ts
import { AbsoluteFill, Series, Audio, staticFile, Loop, OffthreadVideo } from 'remotion';
```
(b) zod 스키마의 `durationInFrames: z.number(),` 다음 줄에 추가:
```ts
    videoSrc: z.string().nullable().optional(),
```
(c) `Scene` 컴포넌트 — 기존:
```tsx
const Scene: React.FC<{ s: AutoShortsProps['scenes'][number]; index: number }> = ({ s, index }) => (
  <AbsoluteFill>
    <KenBurnsImage src={s.imageSrc} total={s.durationInFrames} index={index} />
```
을 다음으로 교체(클립 있으면 루프 재생, 없으면 KenBurns — 씬별 독립 폴백):
```tsx
const CLIP_FRAMES = 180; // 6초 클립 × 30fps 컴포지션 — 씬이 클립보다 길면 루프

const Scene: React.FC<{ s: AutoShortsProps['scenes'][number]; index: number }> = ({ s, index }) => (
  <AbsoluteFill>
    {s.videoSrc ? (
      <Loop durationInFrames={CLIP_FRAMES}>
        <OffthreadVideo src={staticFile(s.videoSrc)} muted style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
      </Loop>
    ) : (
      <KenBurnsImage src={s.imageSrc} total={s.durationInFrames} index={index} />
    )}
```
(나머지 오버레이·자막·오디오 줄은 그대로.)

- [ ] **Step 3: Pass clips from runShortsJob**

`src/orchestrator/shorts.ts` — 기존:
```ts
      try { r = await renderShortsVideoRemotion(dir, plan.scenes, images, { signal: opts.signal }); }
```
을 다음으로 교체:
```ts
      try { r = await renderShortsVideoRemotion(dir, plan.scenes, images, { clips: cv.clips, signal: opts.signal }); }
```

- [ ] **Step 4: Verify types + full tests**

Run: `npx tsc --noEmit && npx tsc -p remotion/tsconfig.json; echo "tsc: $?"` → `tsc: 0`.
Run: `npx vitest run src/tools src/orchestrator` → PASS(회귀 없음 — FAL_KEY 없는 환경에선 clips 전부 null → videoSrc null → KenBurns 경로 그대로).

- [ ] **Step 5: 실런 검증(선택 — FAL_KEY 필요, fal 과금 편당 ~$1 + gpt-image·TTS)**

`.env` 의 `FAL_KEY` 가 채워져 있고 서버가 재시작된 상태에서:
```bash
curl -s -X POST http://127.0.0.1:8787/shorts -H 'content-type: application/json' -d '{"topic":"실내 화분 흙 갈아주는 3단계","scenes":3}'
```
ready 후 확인: ① 활동 피드에 "씬 I2V — N장 클립화" ② `data/shorts/<id>/clips/clip_NN.mp4` 존재 + **ffprobe 로 세로 비율 확인(스펙 §4.4 — 가로면 SHORTS_I2V_MODEL 을 Wan 계열로 교체)** ③ `final.mp4` 재생에서 배경 모션. FAL_KEY 없으면 "씬 I2V" 로그 없이 기존 렌더와 동일한 것이 정상.

- [ ] **Step 6: Commit**

```bash
git status --short   # 아래 3개 파일만 확인
git add src/tools/shortsRenderRemotion.ts remotion/AutoShorts.tsx src/orchestrator/shorts.ts
git commit -m "feat(shorts): Remotion 씬 배경 I2V 클립 재생(videoSrc·Loop·OffthreadVideo) + clips 배선"
```

---

## 완료 기준 (스펙 §8)

- [ ] FAL_KEY 있는 환경 실런에서 씬 배경이 모션 클립으로 재생(세로 비율 확인 포함 — §4.4).
- [ ] 키 없음·`SHORTS_I2V=off`·클립 실패 씬에서 기존 KenBurns 렌더 그대로(회귀 없음).
- [ ] `buildMotionPrompt`·`extractVideoUrl` 단위테스트 통과, 루트+remotion tsc 0.
